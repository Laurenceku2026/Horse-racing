# Copy relevant keys into Streamlit Cloud → App settings → Secrets
# Do NOT commit real passwords into the git repo.

# --- App public URL (password-reset email links) ---
APP_BASE_URL = "https://hkjc-horse-racing.streamlit.app"

# --- Gmail SMTP (forgot-password emails) ---
# Use a Google Account App Password (2FA required), NOT the normal login password.
# Google may show it as 4 groups with spaces; paste with or without spaces.
SMTP_HOST = "smtp.gmail.com"
SMTP_PORT = "587"
SMTP_USER = "Techlife2027@gmail.com"
SMTP_PASSWORD = "your-gmail-app-password"
SMTP_FROM = "Techlife2027@gmail.com"
SMTP_USE_TLS = "1"
